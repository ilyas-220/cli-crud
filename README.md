# CLI CRUD Node.js

A simple Command Line Interface (CLI) application built with Node.js
to perform Create, Read, Update, and Delete operations using JSON file storage.

## Features

- Create data
- Read data
- Update data
- Delete data

## How to Run

```bash
node index.js
```
